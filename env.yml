---
runmode: dev
# production configuration
prod:
    mysql:
        host: localhost:3306
        user: root
        password: prashantpatel
        database: production
    base_url: "http//localhost:8080/"
    providermode: prod
    
# local configuration
dev:
    mysql:
        host: localhost:3306
        user: root
        password: prashantpatel
        database: production
    base_url: "http//localhost:8080/"
    providermode: test

